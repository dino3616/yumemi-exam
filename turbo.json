{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "generate": {},
    "lint": {
      "dependsOn": ["^generate"],
      "outputs": ["$api.*"]
    },
    "fmt": {
      "dependsOn": ["^generate"],
      "outputs": ["$api.*"]
    },
    "style": {
      "dependsOn": ["^generate"],
      "outputs": ["$api.*"]
    },
    "build": {
      "dependsOn": ["^generate", "^build"],
      "outputs": [".next/**", "$api.*", "dist/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
