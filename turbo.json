{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {},
    "fmt": {},
    "style": {},
    "generate": {},
    "build": {
      "dependsOn": ["generate", "^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}
